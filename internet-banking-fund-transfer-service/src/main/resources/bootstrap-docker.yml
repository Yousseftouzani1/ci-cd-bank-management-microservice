spring:
  application:
    name: fund-transfer-service
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

eureka:
  client:
    enabled: false

zipkin:
  enabled: false

flyway:
  enabled: true

spring:
  application:
    name: core-banking-service
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
eureka:
  client:
    enabled: false
zipkin:
    enabled: false
flyway:
    enabled: true